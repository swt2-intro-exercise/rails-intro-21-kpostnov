<h1>Editing Paper</h1>

<%= render 'form', paper: @paper %>

<%= select_tag 'author_ids',
  options_from_collection_for_select(Author.all, :id, :name, @paper.author_ids),
  
  multiple: true, size: 10 %>

<br>

<%= link_to 'Show', @paper %> |
<%= link_to 'Back', papers_path %>
